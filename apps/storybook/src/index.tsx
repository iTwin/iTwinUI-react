// This file is required for CRA. It deliberately doesn't do anything meaningful.

import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(<main />, document.getElementById('root'));
