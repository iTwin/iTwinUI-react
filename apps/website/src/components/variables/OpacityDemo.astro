<article data-iui-theme='dark'>
  {
    [...Array(6)].map((_, i) => (
      <div style={`--_opacity: var(--iui-opacity-${i + 1})`}>{i + 1}</div>
    ))
  }
</article>

<style>
  article {
    border-radius: var(--space-2);
    border: 1px solid var(--color-line-2);
    display: flex;
    justify-content: center;
    gap: var(--space-3);
    padding: var(--space-3);
  }

  div {
    width: var(--space-8);
    height: var(--space-8);
    border-radius: var(--space-1);
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--iui-color-background-1);
    color: var(--iui-color-foreground-2);
    opacity: var(--_opacity);
  }
</style>
